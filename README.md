# Heeij — Homepage

Static marketing/landing page for **Heeij**, served via GitHub Pages.
Email signup is wired to Brevo (EU) with consent + confirmation state.

Brand assets (favicon, og-image, social covers + profile) are generated files —
the canonical design guide (colors, typography, logo rules, slogan) lives in
Notion: "Heeij — Business" → Brand. Keep this repo's tokens in sync with it.

Main (private) repo: the Heeij app + docs live separately (local sibling checkout: `../app`).
